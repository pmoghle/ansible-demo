pipeline{
    agent any
    stages{
       stage('scm checkout'){
          steps{
              git ' 
      }

  }

       stage('create s3 bucket') {
          steps{
        ansible-playbook ansible-playbook.yml
     }
   }
 }
}
